/**
 * Root page — `/`
 *
 * This file intentionally contains only a single import.
 *
 * A placeholder landing page is rendered here as a demonstration starting point.
 * When you are ready to build your own product page, remove the import below
 * and replace it with your own implementation.
 *
 * The placeholder's visible text, links, and section toggles are all managed
 * through `@/components/landing/landingContent.ts` — no component code needs
 * to be touched to customize copy or hide sections.
 */

import PlaceholderLandingPage from '@/components/landing/PlaceholderLandingPage'

export default function Page() {
  return <PlaceholderLandingPage />
}
